import numpy as np
from var13 import shift_matrix
from var16 import count_rows_with_zeros, longest_series_column
from var18 import sort_rows_by_repeats, find_first_non_negative_column


#var13
matrix1 = np.array([
    [9, 5, 2, 9],
    [4, 9, 5, 6],
    [4, 6, 9, 9]
])
shift_amount = 2
mode = 'right'
shifted_matrix = shift_matrix(matrix1, shift_amount, mode)
print("Initial matrix:")
print(matrix1)
print(f"\nMatrix after {shift_amount}-shift {mode}:")
print(shifted_matrix)

#var16
matrix2 = np.array([
    [8, 6, 2, 1],
    [2, 6, 1, 2],
    [0, 9, 4, 5],
    [4, 4, 0, 6]
])
rows_with_zeros = count_rows_with_zeros(matrix2)
best_column = longest_series_column(matrix2)
print("\nNumber of rows with at least one null element:", rows_with_zeros)
print("The number of the column with the longest series of identical elements:", best_column)

#var18
matrix3 = np.array([
    [0, 2, 7, -9],
    [4, 4, 4, 5],
    [9, -7, 2, 4],
    [7, 1, 3, 3]
])
sorted_matrix = sort_rows_by_repeats(matrix3)
first_non_negative_col = find_first_non_negative_column(sorted_matrix)
print("\nSorted matrix:")
print(sorted_matrix)
print("The first column has no negative elements:", first_non_negative_col)
